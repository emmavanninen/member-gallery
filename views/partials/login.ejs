<div class="nav-item dropdown">
    
            <% for (let i of errors) { %>
            <div class="alert alert-danger">
                <%= i%>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span></button>
            </div>
            <% } %>
            
            <% for (let s of success) { %>
            <div class="alert alert-success">
                <%= s %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span></button>
            </div>
            <% } %>

    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true"
        aria-expanded="false">
        Login
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <input type="text" id="inputEmail" class="dropdown-item form-control" placeholder="Email address" name="email">
        <input type="text" id="inputPassword" class="dropdown-item form-control" placeholder="Password" name="password">
    </div>
</div>